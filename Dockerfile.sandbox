# Minimal image the Breaker uses to run a repo's tests against an applied patch.
# ponytail: preinstalled common deps because --network=none forbids pip install at runtime;
# add your target repos' test deps here.
FROM python:3.12-slim
RUN pip install --no-cache-dir pytest pytest-asyncio fastapi httpx sqlalchemy
